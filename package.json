{
  "name": "pusher",
  "version": "1.0.0",
  "description": "push notification to client (web and mobile)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jet",
  "license": "ISC",
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
